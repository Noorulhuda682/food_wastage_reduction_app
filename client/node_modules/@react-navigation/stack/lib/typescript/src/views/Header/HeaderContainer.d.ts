import { Animated, StyleProp, ViewStyle } from 'react-native';
import { Route } from '@react-navigation/native';
import type { Layout, Scene, StackHeaderStyleInterpolator } from '../../types';
export declare type Props = {
    mode: 'float' | 'screen';
    layout: Layout;
    scenes: (Scene | undefined)[];
    getPreviousScene: (props: {
        route: Route<string>;
    }) => Scene | undefined;
    getFocusedRoute: () => Route<string>;
    onContentHeightChange?: (props: {
        route: Route<string>;
        height: number;
    }) => void;
    styleInterpolator: StackHeaderStyleInterpolator;
    style?: Animated.WithAnimatedValue<StyleProp<ViewStyle>>;
};
export default function HeaderContainer({ mode, scenes, layout, getPreviousScene, getFocusedRoute, onContentHeightChange, styleInterpolator, style, }: Props): JSX.Element;
